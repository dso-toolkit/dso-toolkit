language: node_js
node_js:
  - "node"
  - "10"
install:
  - gem install percy-cli
  - npm install
script:
  - npm run build
  - percy snapshot --widths "380,780,1024,1280" --baseurl "/components/preview" build/library/
