import { StorybookComponent } from "@site/src/components/StorybookComponent";

# Segmented Button

<StorybookComponent variant="default" />

Standaard is er geen actieve button. Alleen bij een segmented button is er een actieve optie.
Andere optie:

- WithDisabledButton
