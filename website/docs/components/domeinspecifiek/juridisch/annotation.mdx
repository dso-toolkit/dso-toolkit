import { StorybookComponent } from "@site/src/components/StorybookComponent";

# Annotation

## Activiteit

<StorybookComponent name="annotation" implementations={["core"]} variant="activiteit" />

## Gebiedsaanwijzing

<StorybookComponent name="annotation" implementations={["core"]} variant="gebiedsaanwijzing" />

## Kaart

<StorybookComponent name="annotation" implementations={["core"]} variant="kaart" />

## Locatie

<StorybookComponent name="annotation" implementations={["core"]} variant="locatie" />

## Omgevingsnormwaarde

<StorybookComponent name="annotation" implementations={["core"]} variant="omgevingsnormwaarde" />
