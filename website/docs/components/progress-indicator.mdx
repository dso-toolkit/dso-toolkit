import { StorybookComponent } from "@site/src/components/StorybookComponent";

# Progress Indicator

<StorybookComponent variant="medium" implementations={["core", "react", "angular"]} />

De HTML/CSS implementatie is **Deprecated**.

<StorybookComponent variant="medium" implementations={["html-css"]} name="progress-indicator-deprecated" />

De progress indicator is er in drie groottes (`small`: 24px, `medium`: 32px, `large`: 48px);
