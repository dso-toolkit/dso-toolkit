---
authors: [iterox, cvanderleij]
---

# DSO Toolkit v76.0.0 💷

Deze release bevat **Breaking** changes voor de volgende componenten:

- Document Component
- Pagination Component (HTML/CSS implementatie)

Daarnaast is in deze release is de ondersteuning voor Angular 16 en 17 komen te vervallen.
Vanaf deze release ondersteunt DSO Toolkit Angular versies 18, 19 en 20.

<!-- truncate -->

## Document Component

De deprecated properties `label`, `nummer` en `opschrift` zijn verwijderd. In versie 🪗 68.5.0 is de property `kop` geïntroduceerd. `kop` bevat `label`, `nummer` en `opschrift`.

```html
❌ < 76.0.0
<dso-document-component label="..." nummer="..." opschrift="..." [...]></dso-document-component>

✅ >= 76.0.0
<dso-document-component kop="..." [...]></dso-document-component>
```

## Pagination HTML/CSS Component

De Pagination (HTML/CSS implementatie) wordt niet gebruikt. Daarom hebben we het in deze release verwijderd.

```html
❌
<nav class="pagination"></nav>
```
