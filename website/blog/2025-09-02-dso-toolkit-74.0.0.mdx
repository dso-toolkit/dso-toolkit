---
authors: [cvanderleij]
---

# DSO Toolkit v74.0.0 🐻

## Breaking change

In deze release is in het component `modal` de prop `showCloseButton` hernoemd naar `closable` die default op `false` staat. Stencil verbiedt het direct instellen van een prop-defaultwaarde op `true` in de @Prop()-decorator.


