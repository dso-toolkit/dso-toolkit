import { ArgTypes } from "storybook/internal/types";

import { {{ pascalCase name }} } from "./{{ kebabCase name }}.models.js";

export interface {{ pascalCase name }}Args {
}

export const {{ camelCase name }}Args: {{ pascalCase name }}Args = {
};

export const {{ camelCase name}}ArgTypes: ArgTypes<{{ pascalCase name }}Args> = {
};

export function {{ camelCase name }}ArgsMapper(a: {{ pascalCase name }}Args): {{ pascalCase name }} {
  return {
    ...a
  };
}
