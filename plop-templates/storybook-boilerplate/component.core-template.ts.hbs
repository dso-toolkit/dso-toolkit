import { {{ pascalCase name }} } from "dso-toolkit";
import { html } from "lit-html";

import { ComponentImplementation } from "../../templates";

export const core{{ pascalCase name }}: ComponentImplementation<{{ pascalCase name }}> = {
  component: "{{ camelCase name }}",
  implementation: "core",
  template: () =>
    function {{ camelCase name }}Template({  }) {
      return html`<dso-{{ kebabCase name }}></dso-{{ kebabCase name }}>`;
    },
};
