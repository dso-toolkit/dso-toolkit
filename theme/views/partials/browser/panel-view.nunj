<div data-role="tab-panel" id="browser-{{ entity.id }}-panel-view" aria-labelledby="view-tab">
  <div>
    <pre>{{ entity.getPreviewContent() | async | trim | highlight('nunj') | linkRefs(entity) }}</pre>
  </div>
</div>
