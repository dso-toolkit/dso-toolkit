<div class="dropdown open">
  <button class="btn btn-default dropdown-toggle" type="button" id="{{ id }}" aria-haspopup="true" aria-expanded="true">
    {{ label }}
    <span class="caret"></span>
  </button>
  <ul class="dropdown-menu" aria-labelledby="{{ id }}">
    {{#each items }}
      {{#unless @first }}
        <li role="separator" class="divider"></li>
      {{/unless}}
      {{#each this }}
        <li>
          <a href="#">{{ this }}</a>
        </li>
      {{/each}}
    {{/each}}
  </ul>
</div>
