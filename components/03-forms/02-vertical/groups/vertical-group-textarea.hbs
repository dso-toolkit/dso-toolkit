<div class="form-group textarea">
  <label for="{{ id }}">
    {{ label }}
    {{#unless required }}
      <span class="optional">optioneel</span>
    {{/unless}}
  </label>
  <div {{#if inputIcon }}class="has-feedback"{{/if}}>
    {{#if helpIcon }}
      <button class="input-help"></button>
    {{/if}}
    <textarea class="form-control{{ inputModifiers }}" id="{{ id }}" rows="{{ rows }}"
      {{{ifattr 'disabled' disabled }}}
      {{{ifattr 'readonly' readonly }}}
      {{{ifattr 'required' required }}}></textarea>
    {{#if errorText }}
      <i class="error-text">{{ errorText }}</i>
    {{/if}}
    {{#if helpText }}
      {{> '@help-block' }}
    {{/if}}
  </div>
</div>
