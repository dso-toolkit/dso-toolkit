<button type="button"
  {{ className('dso-info-button', [infoOpen, 'dso-open'], [infoSecondary, 'dso-info-secondary']) }}
  {{ attributes([infoOpen, 'aria-expanded', not(not(infoOpen)), true]) }}
>
  <span class="sr-only">
    {%- if infoButtonLabel -%}
      {{ infoButtonLabel }}
    {%- else -%}
      Toelichting bij deze optie
    {%- endif -%}
  </span>
</button>
