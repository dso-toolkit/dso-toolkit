<span {{ className('dso-badge', [modifier, 'badge-' + modifier]) }}>{% if status -%}<span class="sr-only">{{ status }}: </span>{% endif -%}{{ label }}</span>

<dso-badge {{ attributes([status, 'status', status], [modifier, 'level', modifier]) }}>
  {{ label }}
</dso-badge>
