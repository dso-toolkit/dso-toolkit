<main>
  {% render '@form-steps', {title: steps.title, step: steps.step} %}
  <dso-alert status="{{ alert.status }}">
    {{ alert.message | safe }}
  </dso-alert>
  {% render '@form', form %}
</main>
